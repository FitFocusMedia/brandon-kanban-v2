{
  "name": "brandon-kanban-v2",
  "version": "1.0.0",
  "description": "Brandon's Kanban Board - V2 (GitHub Pages + Supabase)",
  "scripts": {
    "migrate": "node migrate-schema.js",
    "migrate-data": "node migrate-data.js",
    "dev": "serve public",
    "build": "echo 'Static site - no build needed'",
    "deploy": "gh-pages -d public"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "gh-pages": "^6.1.1"
  }
}
